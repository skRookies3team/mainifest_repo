apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mate-service.fullname" . }}-test-connection"
  labels:
    {{- include "mate-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mate-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
