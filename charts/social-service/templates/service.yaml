apiVersion: v1
kind: Service
metadata:
  name: {{ include "social-service.fullname" . }}
  labels:
    {{- include "social-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "social-service.selectorLabels" . | nindent 4 }}
