apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "social-service.fullname" . }}-test-connection"
  labels:
    {{- include "social-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "social-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
